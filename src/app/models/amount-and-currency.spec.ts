import { AmountAndCurrency } from './amount-and-currency';

describe('AmountAndCurrency', () => {
  it('should create an instance', () => {
    expect(new AmountAndCurrency()).toBeTruthy();
  });
});
